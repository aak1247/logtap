## 0.1.0

- Initial release
- Structured logging (debug/info/warn/error/fatal)
- Event tracking (identify/track)
- Batched sending with configurable flush interval and min batch size
- Gzip compression (auto fallback on web)
- Local persistent queue (restart-safe)
- Device ID persistence
- Flutter error capture
